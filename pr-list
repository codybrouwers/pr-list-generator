#!/bin/bash

# PR List Generator - Global Command
# This script runs the TypeScript PR list generator from anywhere

# Get the real path of this script (resolves symlinks)
SCRIPT_PATH="$(readlink -f "${BASH_SOURCE[0]}")"
SCRIPT_DIR="$(dirname "$SCRIPT_PATH")"

exec bun "${SCRIPT_DIR}/generate-pr-list.ts" "$@"
